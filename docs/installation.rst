============
Installation
============

At the command line::

    $ easy_install django-cruds

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-cruds
    $ pip install django-cruds